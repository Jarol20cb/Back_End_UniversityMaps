package com.universitymaps.universitymaps;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class UniversityMapsApplication {

    public static void main(String[] args) {
        SpringApplication.run(UniversityMapsApplication.class, args);
    }
    // check if the project is running ok, but Swagger -> http://localhost:8084/swagger-ui.html
}
